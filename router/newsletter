const express = require("express");
const router = express.Router();
const { newsletterEmail } = require("../controllers/newsletter");

router.post("/newsletterEmail", newsletterEmail);

module.exports = router;
